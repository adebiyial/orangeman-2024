---
import { writing } from "../misc/data";
import { slugify } from "../misc/fns";
import Card from "./Card.astro";
import Hyperlink from "./Hyperlink.astro";
import SectionHeader from "./SectionHeader.astro";

const imageHeight = 40;
---

<section data-grid>
  <SectionHeader
    heading="Writing"
    subheading="I write both technical and non-technical articles."
  />

  <ul data-list>
    {
      writing.map(({ title, subtitle }) => (
        <li data-list-item>
          <Hyperlink href={slugify(title)} nounderline>
            <Card
              {...{
                title,
                subtitle,
                imageUrl: `https://avatar.vercel.sh/${slugify(
                  title
                )}?size=${imageHeight}`,
                imageHeight,
              }}
            />
          </Hyperlink>
        </li>
      ))
    }
  </ul>
</section>

<style>
  [data-list-item] {
    > a {
      text-decoration: none;
    }

    [data-card] {
      grid-template-columns: 40px 1fr;
      align-items: center;

      [data-subtitle] {
        grid-column: 1/-1;
      }
    }
  }
</style>
