<div class="wrap">
  <div class="corner top-left"></div>
  <div class="corner top-right"></div>
  <div class="corner bottom-left"></div>
  <div class="corner bottom-right"></div>
  <div class="card">
    <h1>Hover over the corners</h1>
  </div>
</div>

<style>
  .wrap {
    --bg: #1e3726;
    --transform: perspective(600px);
    --perspective: 600px;
    --rotateX: 0;
    --rotateY: 0;

    height: 400px;
    width: var(--content-width);
    position: relative;

    @media screen and (max-width: 43rem) {
      width: calc(
        100vw - var(--body-margin-left) - var(--body-margin-right) * 2
      );
      height: calc(
        100vw - var(--body-margin-left) - var(--body-margin-right) * 2
      );
    }
  }

  .corner {
    --size: 50%;
    width: var(--size);
    height: var(--size);
    transition: 0.3s;
    position: absolute;
    transform: perspective(var(--perspective)) rotateX(var(--rotateX))
      rotateY(var(--rotateY));
    outline: 1px solid;
    background-color: var(--bg);

    &:hover {
      z-index: 1;
    }
  }

  .top-left {
    inset: 0 auto auto 0;

    &:hover {
      background-color: #eb5024;
      --rotateY: -10deg;
    }
  }

  .top-right {
    inset: 0 0 auto auto;

    &:hover {
      background-color: #6eae26;
      --rotateY: 10deg;
    }
  }

  .top-left,
  .top-right {
    &:hover {
      --rotateX: 10deg;
    }
  }

  .bottom-left {
    inset: auto auto 0 0;

    &:hover {
      background-color: #05a1e9;
      --rotateY: -10deg;
    }
  }

  .bottom-right {
    inset: auto 0 0 auto;

    &:hover {
      background-color: #f7b408;
      --rotateY: 10deg;
    }
  }

  .bottom-left,
  .bottom-right {
    &:hover {
      --rotateX: -10deg;
    }
  }

  .card {
    position: absolute;
    left: 50%;
    top: 50%;
    transform: translate(-50%, -50%);
    z-index: 2;
    background-color: var(--bg);
    padding: 0.5rem;
    text-align: center;
  }
</style>
